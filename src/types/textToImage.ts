export interface TextToImageOptions {
  fontFamily: string,
  fontPath: string, 
  maxWidth: number, 
  textAlign: CanvasTextAlign, 
  verticalAlign: string,
  //customHeight: number,
  lineHeight: number,
  margin: number,
}